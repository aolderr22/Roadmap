Phase 1

STEP 1 — Azure Resources

Created resource group `rg-healthcare-ai`.

Created Azure Storage Account `sthealthcaredocs2026` using Azure Blob Storage / Data Lake Storage Gen2.

Region: East US.

The storage account will serve as the persistent document storage layer for the healthcare RAG project.
---
STEP 2 — Healthcare Document Storage

Created the `healthcare-documents` container in Azure Storage Account `sthealthcaredocs2026`.

Configured the container for private access.

Uploaded one healthcare PDF document to the container.

The documents are now stored in Azure Blob Storage / Data Lake Storage Gen2 and ready to be connected to the RAG retrieval pipeline.
---
STEP 3 — Azure AI Search

Created Azure AI Search service `search-healthcare-ai`.

Resource group: `rg-healthcare-ai`.

Region: Central US.

Pricing tier: Basic.

Central US was used because East US was unavailable for creating a new Azure AI Search service.

Azure AI Search will provide the retrieval layer for the healthcare RAG pipeline.
---
STEP 4 — Microsoft Foundry

Created Microsoft Foundry resource `foundry-healthcare-ai`.

Created Microsoft Foundry project `proj-healthcare-ai`.

Region: East US.

The Foundry project will provide the model and agent infrastructure for the healthcare RAG system.
---
STEP 5 — Embedding Model

Deployed the `text-embedding-3-small` embedding model in Microsoft Foundry.

The deployment is available to the Foundry project and will be used to generate vector embeddings for documents ingested by Azure AI Search.

The project is using the New Foundry experience.
---
STEP 6 — Generation Model

Deployed the `gpt-5-mini` model in Microsoft Foundry.

Deployment name: `gpt-5-mini`.

The model will provide the generation/reasoning layer for the healthcare RAG agent.

The project uses the current New Foundry experience. `gpt-5-mini` was selected instead of the previously considered `gpt-4.1-mini` to avoid building the project around an older model lifecycle.
---
STEP 7 UPDATE — Foundry Project Search Access

Enabled the system-assigned managed identity for the Microsoft Foundry project `proj-healthcare-ai`.

Granted the `Search Index Data Reader` role on Azure AI Search service `search-healthcare-ai` to the managed identity of the Foundry project.

This permission allows the Foundry Agent Service to securely access the Azure AI Search knowledge base through the knowledge base MCP endpoint.
---
STEP 8 — Azure AI Search Blob Knowledge Source

Created Blob Knowledge Source `ks-healthcare-docs` in Azure AI Search.

The knowledge source connects Azure AI Search to the `healthcare-documents` container in storage account `sthealthcaredocs2026`.

Text vectorization is enabled using the embedding model deployed in Microsoft Foundry.

The healthcare PDF has been ingested into the Azure AI Search retrieval pipeline.

Azure AI Search automatically created the indexing pipeline required to make the document available for agentic retrieval.
---
STEP 9 — Azure AI Search Knowledge Base

Created knowledge base `kb-healthcare` in Azure AI Search.

Connected the knowledge base to the Blob Knowledge Source `ks-healthcare-docs`.

Configured `gpt-5-mini` as the model deployment for the knowledge base.

The knowledge base now provides the agentic retrieval layer for the healthcare RAG system, orchestrating retrieval from the healthcare document knowledge source and supporting grounded answer generation.
---
STEP 10 — Foundry RAG Agent

Created the Microsoft Foundry prompt agent `agent-healthcare-rag` using the deployed `gpt-5-mini` model.

Connected the agent to the Azure AI Search knowledge base `kb-healthcare`.

The knowledge base provides grounded retrieval from the healthcare document stored in Azure Blob Storage through the `ks-healthcare-docs` knowledge source.

Tested the agent successfully with questions based on the healthcare document.

Verified that the agent retrieves information from the indexed PDF and can provide answers containing specific numeric evidence from the source.

The Foundry agent currently displays `mcp:answers synthesis` as the citation/reference rather than exposing a direct citation to the underlying PDF. The retrieval and grounding pipeline is nevertheless functioning end-to-end.
---
---

PHASE 1 COMPLETE

Completed the Azure-based healthcare RAG foundation.

The system now consists of:

Healthcare PDF → Azure Blob Storage → Azure AI Search Knowledge Source → chunking and embeddings → Azure AI Search index → Knowledge Base → Foundry IQ/MCP retrieval → Foundry Agent → `gpt-5-mini`.

Verified that the healthcare PDF was successfully indexed into Azure AI Search with 22 indexed chunks.

Verified that each chunk contains a 1536-dimensional vector generated using `text-embedding-3-small`.

Verified that the Foundry Agent can answer questions using evidence from the healthcare document.

The Foundry Agent currently displays `mcp:answers synthesis` rather than a direct citation to the underlying PDF. This does not prevent the RAG pipeline from retrieving and using the document successfully.

Phase 1 is considered complete.

---

PHASE 2

STEP 1 — Python Project Setup

Created the `healthcare-rag-agent` GitHub repository.

Created the initial Python project structure:

healthcare-rag-agent/
├── app/
│   └── main.py
├── .env
├── .gitignore
└── requirements.txt

Configured the Python dependencies required to begin working with Microsoft Foundry.

Created the initial `main.py` application using the Microsoft Foundry Python SDK, Azure Identity, and environment variables.

Installed the project dependencies with `pip install -r requirements.txt`.

A Python virtual environment has not yet been created.

Phase 2 Step 1 is complete. The next step is to create and use a Python virtual environment before running the application and verifying programmatic communication with the existing `agent-healthcare-rag` Foundry agent.

STEP 2 — Python Connection to Foundry Agent

Authenticated to Azure using the Azure CLI with `az login`.

Successfully ran the Python application from the VS Code terminal.

Connected programmatically to the existing Microsoft Foundry project `proj-healthcare-ai`.

Successfully connected to the existing Foundry Agent `agent-healthcare-rag`.

Tested the agent from the Python application with a question about the healthcare document.

The agent successfully returned an answer grounded in the healthcare document.

Step 2 is complete.
---
STEP 3 — Separate Foundry Client Logic

Created `app/foundry_client.py` to encapsulate the Microsoft Foundry project connection and agent client configuration.

Updated `app/main.py` to use the Foundry client instead of containing the connection setup directly.

The application now separates responsibilities:

`main.py` handles command-line interaction, while `foundry_client.py` handles the Microsoft Foundry connection and authentication.

Tested the refactored application successfully from the VS Code terminal.

The application continues to connect to `agent-healthcare-rag` and successfully answer questions using the healthcare document.

Step 3 is complete.
---
STEP 4 — Add Configuration Layer

Created `app/config.py` to centralize application configuration.

Moved the `PROJECT_ENDPOINT` and `AGENT_NAME` environment-variable handling out of `foundry_client.py` and into the configuration layer.

Updated `app/foundry_client.py` to obtain configuration values from `config.py`.

The application now separates configuration, Foundry client infrastructure, and application logic.

Tested the refactored application successfully from the VS Code terminal.

The application continues to connect to `agent-healthcare-rag` and successfully answer questions using the healthcare document.

Step 4 is complete.
---
### Step 5 — FastAPI API

Added FastAPI to the Python application and exposed the Healthcare RAG Agent through an HTTP API.

* Added FastAPI and Uvicorn dependencies.
* Created `app/api.py`.
* Added `GET /health` endpoint.
* Added `POST /ask` endpoint for submitting questions to the Foundry Agent.
* Verified the FastAPI application starts successfully with Uvicorn.
* Verified the API endpoint is accessible through the FastAPI Swagger UI at `/docs`.
* Kept the Foundry client and configuration logic separated in `foundry_client.py` and `config.py`.

The Python application now provides both:

* a terminal-based client through `main.py`
* an HTTP API through FastAPI
---
---
### Step 6 — Dockerize FastAPI Application

Created a Dockerfile for the FastAPI application.

Configured the Docker image to:
* Use Python 3.13 slim as the base image.
* Install the project's Python dependencies.
* Install the Azure CLI inside the container.
* Copy the application code into the image.
* Expose port 8000.
* Start the FastAPI application with Uvicorn using `app.api:app`.

Created a `.dockerignore` file to prevent local development files and environment secrets from being included in the Docker image.

Successfully built the Docker image as `healthcare-rag-agent:latest`.

Configured local Docker authentication by:
* Passing `.env` to the container at runtime rather than including it in the image.
* Mounting the local Azure CLI configuration into the container.
* Using `DefaultAzureCredential` to authenticate the application to Azure.

Successfully started the FastAPI application inside Docker.

Verified that:
* The container starts successfully.
* FastAPI starts successfully under Uvicorn.
* The `/docs` Swagger UI is accessible.
* The `/health` endpoint is available.
* The `/ask` endpoint successfully communicates with the Microsoft Foundry Agent.
* The Dockerized application successfully answers questions using the healthcare RAG system.

Step 6 is complete.
--
### Step 7 — Azure Container Registry (ACR)

Created an Azure Container Registry to store the Docker image for the healthcare RAG FastAPI application.

Configured the registry:

* Resource group: `rg-healthcare-ai`
* Region: East US
* Registry name: `healthcarerag2026`
* SKU: Standard
* Domain name label scope: Tenant Reuse
* Role assignment permissions mode: RBAC Registry + ABAC Repository Permissions
* Login server: `healthcarerag2026-bxgqg5erabcaf2gs.azurecr.io`

Authenticated to Azure Container Registry using the Azure CLI:

```bash
az login
az acr login --name healthcarerag2026
```

Tagged the local Docker image:

```bash
docker tag healthcare-rag-agent:latest healthcarerag2026-bxgqg5erabcaf2gs.azurecr.io/healthcare-rag-agent:latest
```

Because the registry uses RBAC Registry + ABAC Repository Permissions, assigned the `Container Registry Repository Writer` role to the Azure user so the Docker image could be pushed to the registry.

Successfully pushed the Docker image to Azure Container Registry:

```text
healthcarerag2026-bxgqg5erabcaf2gs.azurecr.io/healthcare-rag-agent:latest
```

Verified that the `latest` image tag exists in the `healthcare-rag-agent` repository in ACR.

Step 7 is complete.
--
### Step 8 — Deploy FastAPI Application to Azure Container Apps

Deployed the Dockerized FastAPI healthcare RAG application to Azure Container Apps.

Created and configured:

* Azure Container Apps environment: `healthcare-rag-env`
* Azure Container App: `healthcare-rag-agent`
* Public HTTPS ingress on port 8000
* Azure Container Registry integration using the Container Apps environment managed identity

The original Docker image was built on an Apple Silicon Mac and was ARM64. Azure Container Apps required a Linux AMD64 image, so the application was rebuilt using:

`docker buildx build --platform linux/amd64`

The AMD64 image was pushed to Azure Container Registry and successfully deployed to Azure Container Apps.

Configured the Container App with the required environment variables:

* `PROJECT_ENDPOINT`
* `AGENT_NAME`

Configured a system-assigned managed identity for the Container App.

Granted the Container App managed identity access to the Microsoft Foundry project:

* `Foundry Agent Consumer`
* `Foundry User`

The `Foundry User` role was required because the application creates a conversation during startup using:

`openai.conversations.create()`

Resolved authentication and authorization issues involving `DefaultAzureCredential` and the Container App managed identity.

Verified that:

* The Container App provisioning state is `Succeeded`.
* The Container App is running.
* FastAPI/Uvicorn starts successfully.
* The `/health` endpoint returns `{"status":"healthy"}`.
* The public `/ask` endpoint successfully communicates with the Microsoft Foundry healthcare RAG agent.
* The deployed application successfully answers healthcare questions through the public Azure endpoint.

The application is now running successfully in Azure Container Apps.

Step 8 is complete.

